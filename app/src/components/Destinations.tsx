import {Link} from 'react-router-dom'; import {ArrowRight} from 'lucide-react'; import {STOPS} from '../data'; import {SiteHeader} from './SiteHeader'; import {MobileTabBar} from './MobileTabBar';
export function Destinations(){return <div className="page light-page"><SiteHeader/><main className="content-page"><p className="eyebrow gold-dark">Explore route</p><h1>Eight stops, in the order the train finds them.</h1><p className="intro">Open the live journey to hear each place tell its own story as you pass through it.</p><ol className="dest-list">{STOPS.map((s,i)=><li key={s.id}><Link to="/journey"><img src={s.image}/><div><p className="eyebrow">Stop {i+1} · {s.province} · {s.km} km</p><h2>{s.name}</h2><strong>{s.tagline}</strong><p>{s.teaser}</p><span>Open on the journey map <ArrowRight/></span></div></Link></li>)}</ol></main><MobileTabBar/></div>}
