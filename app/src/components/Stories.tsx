import {Link} from 'react-router-dom'; import {Headphones} from 'lucide-react'; import {STOPS} from '../data'; import {SiteHeader} from './SiteHeader'; import {MobileTabBar} from './MobileTabBar';
export function Stories(){return <div className="page light-page"><SiteHeader/><main className="content-page narrow"><p className="eyebrow gold-dark">Corridor stories</p><h1>Every kilometre belongs to somebody.</h1><p className="intro">Read the current place summaries for each stop. Community voice recordings can be added after contributor consent and editorial review.</p><div className="story-list">{STOPS.map(s=><article key={s.id}><div><p className="eyebrow">{s.name}</p><h2>{s.audioTitle}</h2></div><span aria-hidden="true"><Headphones/></span><p>{s.story.slice(0,220)}…</p><small>{s.audioLength.replace('local story', 'estimated reading')}</small></article>)}</div><section className="dark-cta"><h2>Share a local story</h2><p>If you live along the line, your version of a place is the one travellers want.</p><Link to="/plan">Contribute a story</Link></section></main><MobileTabBar/></div>}
