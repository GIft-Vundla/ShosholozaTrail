import {Link} from 'react-router-dom'; import {STOPS,TOTAL_HOURS,TOTAL_KM} from '../data'; import {SiteHeader} from './SiteHeader'; import {MobileTabBar} from './MobileTabBar';
export function Plan(){return <div className="page light-page"><SiteHeader/><main className="content-page narrow"><p className="eyebrow gold-dark">Plan your trip</p><h1>Two days, {TOTAL_KM} kilometres, one country.</h1><div className="plan-stats">{[['Journey time',TOTAL_HOURS+' hours'],['Best season','March – May'],['Boarding','Pretoria Station']].map(([k,v])=><div key={k}><p className="eyebrow">{k}</p><h3>{v}</h3></div>)}</div><h2 className="timetable-title">Timetable</h2><ul className="timetable">{STOPS.map(s=><li key={s.id}><div><h3>{s.name}</h3><p>{s.tagline}</p></div><div><strong>{s.arrival}</strong><small>{s.km} km</small></div></li>)}</ul><section className="dark-cta"><h2>Ready when you are</h2><p>Ride the route first, save the places you like, and build your itinerary from what you saw out of the window.</p><Link to="/journey">Start the journey</Link></section></main><MobileTabBar/></div>}
